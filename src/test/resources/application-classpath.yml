frontend:
  libs:
    path: /libs
    folder: /static/libs
  app:
    folder: /frontend
    settings: app-settings.json
    components:
      path: /components
      folder: /components
    main:
      path: /app
      folder: /app